# Read by docker compose (this directory is the compose project directory).
# One place to bump: it selects the Playwright image tag *and* the pip version
# installed in the dev container. Client and server must match, or connect()
# fails with a version mismatch.
PLAYWRIGHT_VERSION=1.57.0
